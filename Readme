由于是第一次使用，还存在许多不便，这几个代码是使用的Anaconda集成下的jupyter notebook写的。
第一个文件是爬取安居客每个页面的小区数据并保存为html文件方便随时查。
第二个文件是将每个页面包含的25个小区数据加入到xlsx文件中，并找出对应的每个小区页面地址并保留为html文件。
第三个文件是将每个小区详情页的数据提取需要的信息，例如小区户数，建筑面积。
所有的文件配合在一起是为了爬取安居客某个地区的小区数据信息，如果需要价格什么的，需要自己修改一些地方还有xpath。
代码借鉴了网络上的其他用户的代码。
